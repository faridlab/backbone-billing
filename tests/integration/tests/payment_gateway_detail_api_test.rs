//! PaymentGatewayDetail API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the PaymentGatewayDetail CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// PAYMENTGATEWAYDETAIL TEST DATA GENERATOR
// ============================================================================

/// Test data generator for PaymentGatewayDetail
pub struct PaymentGatewayDetailTestData;

impl TestDataGenerator for PaymentGatewayDetailTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "payment_id": null,
            "external_id": null,
            "invoice_url": null,
            "checkout_url": null,
            "qr_code_url": null,
            "virtual_account_number": null,
            "virtual_account_bank": null,
            "webhook_payload": null,
            "webhook_received_at": now,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "payment_id": null,
            "external_id": null,
            "invoice_url": null,
            "checkout_url": null,
            "qr_code_url": null,
            "virtual_account_number": null,
            "virtual_account_bank": null,
            "webhook_payload": null,
            "webhook_received_at": now,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// PAYMENTGATEWAYDETAIL API TEST
// ============================================================================

/// PaymentGatewayDetail API test suite
pub struct PaymentGatewayDetailApiTest {
    inner: GenericCrudTest<PaymentGatewayDetailTestData>,
}

impl PaymentGatewayDetailApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/payment_gateway_details", "PaymentGatewayDetail");
        Self {
            inner: GenericCrudTest::new(config, PaymentGatewayDetailTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for PaymentGatewayDetailApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_payment_gateway_detail_crud() {
        let mut test = PaymentGatewayDetailApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add PaymentGatewayDetail specific tests here
// <<< CUSTOM TESTS END >>>
