//! PurchaseInvoice API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the PurchaseInvoice CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// PURCHASEINVOICE TEST DATA GENERATOR
// ============================================================================

/// Test data generator for PurchaseInvoice
pub struct PurchaseInvoiceTestData;

impl TestDataGenerator for PurchaseInvoiceTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "invoice_number": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "company_id": Uuid::new_v4().to_string(),
            "branch_id": null,
            "supplier_id": Uuid::new_v4().to_string(),
            "source_po_id": null,
            "status": "draft",
            "posting_date": Utc::now().format("%Y-%m-%d").to_string(),
            "due_date": null,
            "payment_term_id": null,
            "early_pay_discount_percent": 0,
            "early_pay_discount_deadline": null,
            "early_pay_discount_account_id": null,
            "currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "net_total": 0,
            "tax_total": 0,
            "withholding_total": 0,
            "grand_total": 0,
            "outstanding_amount": 0,
            "payable_account_id": Uuid::new_v4().to_string(),
            "posting_state": "pending",
            "journal_id": null,
            "accounting_post_id": null,
            "posted_at": now,
            "notes": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "invoice_number": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "company_id": Uuid::new_v4().to_string(),
            "branch_id": null,
            "supplier_id": Uuid::new_v4().to_string(),
            "source_po_id": null,
            "status": "draft",
            "posting_date": Utc::now().format("%Y-%m-%d").to_string(),
            "due_date": null,
            "payment_term_id": null,
            "early_pay_discount_percent": 0,
            "early_pay_discount_deadline": null,
            "early_pay_discount_account_id": null,
            "currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "net_total": 0,
            "tax_total": 0,
            "withholding_total": 0,
            "grand_total": 0,
            "outstanding_amount": 0,
            "payable_account_id": Uuid::new_v4().to_string(),
            "posting_state": "pending",
            "journal_id": null,
            "accounting_post_id": null,
            "posted_at": now,
            "notes": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// PURCHASEINVOICE API TEST
// ============================================================================

/// PurchaseInvoice API test suite
pub struct PurchaseInvoiceApiTest {
    inner: GenericCrudTest<PurchaseInvoiceTestData>,
}

impl PurchaseInvoiceApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/purchase_invoices", "PurchaseInvoice");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, PurchaseInvoiceTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for PurchaseInvoiceApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_purchase_invoice_crud() {
        let mut test = PurchaseInvoiceApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add PurchaseInvoice specific tests here
// <<< CUSTOM TESTS END >>>
