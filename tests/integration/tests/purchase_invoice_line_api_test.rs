//! PurchaseInvoiceLine API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the PurchaseInvoiceLine CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// PURCHASEINVOICELINE TEST DATA GENERATOR
// ============================================================================

/// Test data generator for PurchaseInvoiceLine
pub struct PurchaseInvoiceLineTestData;

impl TestDataGenerator for PurchaseInvoiceLineTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "invoice_id": Uuid::new_v4().to_string(),
            "company_id": Uuid::new_v4().to_string(),
            "item_id": Uuid::new_v4().to_string(),
            "expense_account_id": Uuid::new_v4().to_string(),
            "description": null,
            "quantity": 0,
            "unit_price": 0,
            "net_amount": 0,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "invoice_id": Uuid::new_v4().to_string(),
            "company_id": Uuid::new_v4().to_string(),
            "item_id": Uuid::new_v4().to_string(),
            "expense_account_id": Uuid::new_v4().to_string(),
            "description": null,
            "quantity": 0,
            "unit_price": 0,
            "net_amount": 0,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }

    async fn seed_dependencies(&self, api: &ApiTest) -> Vec<(String, String)> {
        let mut deps: Vec<(String, String)> = Vec::new();
        if let Some(id) = super::crud_test_base::create_and_get_id(api, "/api/v1/purchase_invoices", &super::purchase_invoice_api_test::PurchaseInvoiceTestData).await {
            deps.push(("invoice_id".to_string(), id));
        }
        deps
    }
}

// ============================================================================
// PURCHASEINVOICELINE API TEST
// ============================================================================

/// PurchaseInvoiceLine API test suite
pub struct PurchaseInvoiceLineApiTest {
    inner: GenericCrudTest<PurchaseInvoiceLineTestData>,
}

impl PurchaseInvoiceLineApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/purchase_invoice_lines", "PurchaseInvoiceLine");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, PurchaseInvoiceLineTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for PurchaseInvoiceLineApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_purchase_invoice_line_crud() {
        let mut test = PurchaseInvoiceLineApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add PurchaseInvoiceLine specific tests here
// <<< CUSTOM TESTS END >>>
