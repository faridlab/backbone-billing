//! PaymentTermLine API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the PaymentTermLine CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// PAYMENTTERMLINE TEST DATA GENERATOR
// ============================================================================

/// Test data generator for PaymentTermLine
pub struct PaymentTermLineTestData;

impl TestDataGenerator for PaymentTermLineTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "term_id": Uuid::new_v4().to_string(),
            "company_id": null,
            "value": "balance",
            "value_amount": 0,
            "nb_days": 1,
            "day_of_month": null,
            "delay_type": "days",
            "anchor": "invoice_date",
            "sequence": 1,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "term_id": Uuid::new_v4().to_string(),
            "company_id": null,
            "value": "balance",
            "value_amount": 0,
            "nb_days": 1,
            "day_of_month": null,
            "delay_type": "days",
            "anchor": "invoice_date",
            "sequence": 1,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }

    async fn seed_dependencies(&self, api: &ApiTest) -> Vec<(String, String)> {
        let mut deps: Vec<(String, String)> = Vec::new();
        if let Some(id) = super::crud_test_base::create_and_get_id(
            api,
            "/api/v1/payment_terms",
            &super::payment_term_api_test::PaymentTermTestData,
        )
        .await
        {
            deps.push(("term_id".to_string(), id));
        }
        deps
    }
}

// ============================================================================
// PAYMENTTERMLINE API TEST
// ============================================================================

/// PaymentTermLine API test suite
pub struct PaymentTermLineApiTest {
    inner: GenericCrudTest<PaymentTermLineTestData>,
}

impl PaymentTermLineApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/payment_term_lines", "PaymentTermLine");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, PaymentTermLineTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for PaymentTermLineApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_payment_term_line_crud() {
        let mut test = PaymentTermLineApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add PaymentTermLine specific tests here
// <<< CUSTOM TESTS END >>>
