//! AgentEarning API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the AgentEarning CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// AGENTEARNING TEST DATA GENERATOR
// ============================================================================

/// Test data generator for AgentEarning
pub struct AgentEarningTestData;

impl TestDataGenerator for AgentEarningTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "agent_id": null,
            "task_id": null,
            "order_id": null,
            "earning_type": null,
            "description": "Test description",
            "currency": null,
            "base_amount": null,
            "distance_km": null,
            "distance_bonus": null,
            "time_bonus": null,
            "surge_bonus": null,
            "incentive_bonus": null,
            "tip_amount": null,
            "deduction_amount": null,
            "deduction_reason": null,
            "total_earning": null,
            "status": null,
            "earned_at": now,
            "earning_date": null,
            "is_paid": false,
            "payout_id": null,
            "paid_at": now,
            "cod_collected": null,
            "cod_deposited": null,
            "cod_deposited_at": now,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "agent_id": null,
            "task_id": null,
            "order_id": null,
            "earning_type": null,
            "description": "Test description",
            "currency": null,
            "base_amount": null,
            "distance_km": null,
            "distance_bonus": null,
            "time_bonus": null,
            "surge_bonus": null,
            "incentive_bonus": null,
            "tip_amount": null,
            "deduction_amount": null,
            "deduction_reason": null,
            "total_earning": null,
            "status": null,
            "earned_at": now,
            "earning_date": null,
            "is_paid": false,
            "payout_id": null,
            "paid_at": now,
            "cod_collected": null,
            "cod_deposited": null,
            "cod_deposited_at": now,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// AGENTEARNING API TEST
// ============================================================================

/// AgentEarning API test suite
pub struct AgentEarningApiTest {
    inner: GenericCrudTest<AgentEarningTestData>,
}

impl AgentEarningApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/agent_earnings", "AgentEarning");
        Self {
            inner: GenericCrudTest::new(config, AgentEarningTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for AgentEarningApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_agent_earning_crud() {
        let mut test = AgentEarningApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add AgentEarning specific tests here
// <<< CUSTOM TESTS END >>>
