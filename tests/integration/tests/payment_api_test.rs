//! Payment API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the Payment CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// PAYMENT TEST DATA GENERATOR
// ============================================================================

/// Test data generator for Payment
pub struct PaymentTestData;

impl TestDataGenerator for PaymentTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "payment_number": null,
            "payable_type": null,
            "payable_id": null,
            "order_id": null,
            "payer_type": null,
            "payer_id": null,
            "payee_type": null,
            "payee_id": null,
            "currency": null,
            "amount": null,
            "net_amount": null,
            "gateway": null,
            "channel": null,
            "status": null,
            "expires_at": now,
            "paid_at": now,
            "failed_at": now,
            "cancelled_at": now,
            "failure_code": null,
            "failure_message": null,
            "settlement_id": null,
            "description": "Test description",
            "notes": null,
            "data": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "payment_number": null,
            "payable_type": null,
            "payable_id": null,
            "order_id": null,
            "payer_type": null,
            "payer_id": null,
            "payee_type": null,
            "payee_id": null,
            "currency": null,
            "amount": null,
            "net_amount": null,
            "gateway": null,
            "channel": null,
            "status": null,
            "expires_at": now,
            "paid_at": now,
            "failed_at": now,
            "cancelled_at": now,
            "failure_code": null,
            "failure_message": null,
            "settlement_id": null,
            "description": "Test description",
            "notes": null,
            "data": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// PAYMENT API TEST
// ============================================================================

/// Payment API test suite
pub struct PaymentApiTest {
    inner: GenericCrudTest<PaymentTestData>,
}

impl PaymentApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/payments", "Payment");
        Self {
            inner: GenericCrudTest::new(config, PaymentTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for PaymentApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_payment_crud() {
        let mut test = PaymentApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add Payment specific tests here
// <<< CUSTOM TESTS END >>>
