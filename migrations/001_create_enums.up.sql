-- Enum types for billing module
-- Generated by backbone-schema

-- Create earning_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'earning_type') THEN
        CREATE TYPE earning_type AS ENUM ('pickup', 'delivery', 'pickup_delivery', 'bonus', 'tip', 'incentive', 'adjustment', 'referral');
    END IF;
END
$$;

-- Create earning_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'earning_status') THEN
        CREATE TYPE earning_status AS ENUM ('pending', 'confirmed', 'paid', 'cancelled', 'disputed');
    END IF;
END
$$;

-- Create financial_period_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'financial_period_type') THEN
        CREATE TYPE financial_period_type AS ENUM ('daily', 'weekly', 'monthly', 'quarterly', 'yearly');
    END IF;
END
$$;

-- Create financial_period_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'financial_period_status') THEN
        CREATE TYPE financial_period_status AS ENUM ('open', 'closing', 'closed', 'locked');
    END IF;
END
$$;

-- Create payable_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'payable_type') THEN
        CREATE TYPE payable_type AS ENUM ('order', 'subscription', 'registration', 'corporate_invoice');
    END IF;
END
$$;

-- Create payer_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'payer_type') THEN
        CREATE TYPE payer_type AS ENUM ('customer', 'corporate', 'provider');
    END IF;
END
$$;

-- Create payee_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'payee_type') THEN
        CREATE TYPE payee_type AS ENUM ('provider', 'platform');
    END IF;
END
$$;

-- Create payment_transaction_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'payment_transaction_status') THEN
        CREATE TYPE payment_transaction_status AS ENUM ('pending', 'processing', 'completed', 'failed', 'expired', 'cancelled', 'refunded', 'partially_refunded');
    END IF;
END
$$;

-- Create payment_gateway enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'payment_gateway') THEN
        CREATE TYPE payment_gateway AS ENUM ('xendit', 'midtrans', 'doku', 'stripe', 'manual');
    END IF;
END
$$;

-- Create payment_channel enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'payment_channel') THEN
        CREATE TYPE payment_channel AS ENUM ('qris', 'va_bca', 'va_bni', 'va_bri', 'va_mandiri', 'va_permata', 'va_cimb', 'ovo', 'gopay', 'dana', 'shopeepay', 'linkaja', 'credit_card', 'debit_card', 'cash', 'bank_transfer');
    END IF;
END
$$;

-- Create payment_fee_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'payment_fee_type') THEN
        CREATE TYPE payment_fee_type AS ENUM ('service_fee', 'gateway_fee', 'tax');
    END IF;
END
$$;

-- Create invoiceable_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'invoiceable_type') THEN
        CREATE TYPE invoiceable_type AS ENUM ('order', 'settlement', 'subscription', 'corporate_invoice');
    END IF;
END
$$;

-- Create invoice_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'invoice_type') THEN
        CREATE TYPE invoice_type AS ENUM ('order', 'subscription', 'registration', 'settlement', 'corporate');
    END IF;
END
$$;

-- Create invoice_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'invoice_status') THEN
        CREATE TYPE invoice_status AS ENUM ('draft', 'sent', 'viewed', 'paid', 'partially_paid', 'overdue', 'cancelled', 'void');
    END IF;
END
$$;

-- Create invoice_party_role enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'invoice_party_role') THEN
        CREATE TYPE invoice_party_role AS ENUM ('issuer', 'recipient');
    END IF;
END
$$;

-- Create invoice_party_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'invoice_party_type') THEN
        CREATE TYPE invoice_party_type AS ENUM ('platform', 'provider', 'customer', 'corporate');
    END IF;
END
$$;

-- Create bank_account_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'bank_account_type') THEN
        CREATE TYPE bank_account_type AS ENUM ('checking', 'savings', 'business');
    END IF;
END
$$;

-- Create bank_account_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'bank_account_status') THEN
        CREATE TYPE bank_account_status AS ENUM ('pending', 'verified', 'rejected', 'suspended', 'inactive');
    END IF;
END
$$;

-- Create verification_method enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'verification_method') THEN
        CREATE TYPE verification_method AS ENUM ('micro_deposit', 'document', 'instant', 'manual');
    END IF;
END
$$;

-- Create refund_method enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'refund_method') THEN
        CREATE TYPE refund_method AS ENUM ('original', 'bank_transfer', 'ewallet');
    END IF;
END
$$;

-- Create refund_reason enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'refund_reason') THEN
        CREATE TYPE refund_reason AS ENUM ('customer_request', 'order_cancelled', 'service_issue', 'item_damaged', 'item_lost', 'overcharge', 'duplicate_payment', 'provider_unable', 'other');
    END IF;
END
$$;

-- Create refund_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'refund_status') THEN
        CREATE TYPE refund_status AS ENUM ('pending', 'under_review', 'approved', 'processing', 'completed', 'rejected', 'failed', 'cancelled');
    END IF;
END
$$;

-- Create settlement_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'settlement_status') THEN
        CREATE TYPE settlement_status AS ENUM ('pending', 'processing', 'completed', 'failed', 'on_hold');
    END IF;
END
$$;

-- Create settlement_frequency enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'settlement_frequency') THEN
        CREATE TYPE settlement_frequency AS ENUM ('daily', 'weekly', 'biweekly', 'monthly');
    END IF;
END
$$;

