//! Repository for Invoice entities
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<Invoice, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::Invoice;

/// Table name for Invoice entities
pub const TABLE_NAME: &str = "invoices";

/// Repository for Invoice entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct InvoiceRepository(
    backbone_orm::GenericCrudRepository<Invoice, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for InvoiceRepository {
    type Target = backbone_orm::GenericCrudRepository<Invoice, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl InvoiceRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

impl InvoiceRepository {
    /// Find invoice by invoice_number (excludes soft-deleted)
    pub async fn find_by_invoice_number(&self, invoice_number: &str) -> Result<Option<Invoice>> {
        self.find_by_text_field("invoice_number", invoice_number).await
    }

    /// Check if invoice exists by invoice_number (excludes soft-deleted)
    pub async fn exists_by_invoice_number(&self, invoice_number: &str) -> Result<bool> {
        self.exists_by_text_field("invoice_number", invoice_number).await
    }

}

backbone_core::impl_crud_repository!(InvoiceRepository, Invoice, soft_delete);
