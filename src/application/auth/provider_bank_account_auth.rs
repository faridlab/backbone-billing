//! Auth configuration for ProviderBankAccount handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<ProviderBankAccount>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::ProviderBankAccountService;
use crate::domain::entity::ProviderBankAccount;

/// Permission string constants for ProviderBankAccount operations.
pub mod permissions {
    pub const LIST:        &str = "provider_bank_account:list";
    pub const READ:        &str = "provider_bank_account:read";
    pub const CREATE:      &str = "provider_bank_account:create";
    pub const UPDATE:      &str = "provider_bank_account:update";
    pub const DELETE:      &str = "provider_bank_account:delete";
    pub const RESTORE:     &str = "provider_bank_account:restore";
    pub const TRASH:       &str = "provider_bank_account:trash";
    pub const EMPTY_TRASH: &str = "provider_bank_account:empty_trash";
    pub const BULK_CREATE: &str = "provider_bank_account:bulk_create";
    pub const UPSERT:      &str = "provider_bank_account:upsert";
}

/// Resource policy for ProviderBankAccount — maps CRUD actions to permission strings.
pub struct ProviderBankAccountPolicy;

#[async_trait]
impl ResourcePolicy<ProviderBankAccount> for ProviderBankAccountPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &ProviderBankAccount,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for ProviderBankAccount handlers.
///
/// Wraps `ProviderBankAccountService` and checks `ProviderBankAccountPolicy` before delegating.
pub type ProviderBankAccountGuard = PermissionGuard<ProviderBankAccount, ProviderBankAccountService, ProviderBankAccountPolicy>;

/// Service-integrated permission guard for ProviderBankAccount (for use in generated handlers).
pub type ProviderBankAccountServiceGuard = ServicePermissionGuard<ProviderBankAccount, ProviderBankAccountService, ProviderBankAccountPolicy>;

// <<< CUSTOM
// END CUSTOM
