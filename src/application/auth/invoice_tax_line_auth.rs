//! Auth configuration for InvoiceTaxLine handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<InvoiceTaxLine>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::InvoiceTaxLineService;
use crate::domain::entity::InvoiceTaxLine;

/// Permission string constants for InvoiceTaxLine operations.
pub mod permissions {
    pub const LIST:        &str = "invoice_tax_line:list";
    pub const READ:        &str = "invoice_tax_line:read";
    pub const CREATE:      &str = "invoice_tax_line:create";
    pub const UPDATE:      &str = "invoice_tax_line:update";
    pub const DELETE:      &str = "invoice_tax_line:delete";
    pub const RESTORE:     &str = "invoice_tax_line:restore";
    pub const TRASH:       &str = "invoice_tax_line:trash";
    pub const EMPTY_TRASH: &str = "invoice_tax_line:empty_trash";
    pub const BULK_CREATE: &str = "invoice_tax_line:bulk_create";
    pub const UPSERT:      &str = "invoice_tax_line:upsert";
}

/// Resource policy for InvoiceTaxLine — maps CRUD actions to permission strings.
pub struct InvoiceTaxLinePolicy;

#[async_trait]
impl ResourcePolicy<InvoiceTaxLine> for InvoiceTaxLinePolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &InvoiceTaxLine,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for InvoiceTaxLine handlers.
///
/// Wraps `InvoiceTaxLineService` and checks `InvoiceTaxLinePolicy` before delegating.
pub type InvoiceTaxLineGuard = PermissionGuard<InvoiceTaxLine>;

/// Service-integrated permission guard for InvoiceTaxLine (for use in generated handlers).
pub type InvoiceTaxLineServiceGuard = ServicePermissionGuard<InvoiceTaxLine, InvoiceTaxLineService, InvoiceTaxLinePolicy>;

// <<< CUSTOM
// END CUSTOM
