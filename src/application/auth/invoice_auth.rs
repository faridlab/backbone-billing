//! Auth configuration for Invoice handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<Invoice>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::InvoiceService;
use crate::domain::entity::Invoice;

/// Permission string constants for Invoice operations.
pub mod permissions {
    pub const LIST:        &str = "invoice:list";
    pub const READ:        &str = "invoice:read";
    pub const CREATE:      &str = "invoice:create";
    pub const UPDATE:      &str = "invoice:update";
    pub const DELETE:      &str = "invoice:delete";
    pub const RESTORE:     &str = "invoice:restore";
    pub const TRASH:       &str = "invoice:trash";
    pub const EMPTY_TRASH: &str = "invoice:empty_trash";
    pub const BULK_CREATE: &str = "invoice:bulk_create";
    pub const UPSERT:      &str = "invoice:upsert";
}

/// Resource policy for Invoice — maps CRUD actions to permission strings.
pub struct InvoicePolicy;

#[async_trait]
impl ResourcePolicy<Invoice> for InvoicePolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &Invoice,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for Invoice handlers.
///
/// Wraps `InvoiceService` and checks `InvoicePolicy` before delegating.
pub type InvoiceGuard = PermissionGuard<Invoice, InvoiceService, InvoicePolicy>;

/// Service-integrated permission guard for Invoice (for use in generated handlers).
pub type InvoiceServiceGuard = ServicePermissionGuard<Invoice, InvoiceService, InvoicePolicy>;

// <<< CUSTOM
// END CUSTOM
