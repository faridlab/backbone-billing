//! Auth configuration for SalesInvoiceLine handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<SalesInvoiceLine>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::SalesInvoiceLineService;
use crate::domain::entity::SalesInvoiceLine;

/// Permission string constants for SalesInvoiceLine operations.
pub mod permissions {
    pub const LIST:        &str = "sales_invoice_line:list";
    pub const READ:        &str = "sales_invoice_line:read";
    pub const CREATE:      &str = "sales_invoice_line:create";
    pub const UPDATE:      &str = "sales_invoice_line:update";
    pub const DELETE:      &str = "sales_invoice_line:delete";
    pub const RESTORE:     &str = "sales_invoice_line:restore";
    pub const TRASH:       &str = "sales_invoice_line:trash";
    pub const EMPTY_TRASH: &str = "sales_invoice_line:empty_trash";
    pub const BULK_CREATE: &str = "sales_invoice_line:bulk_create";
    pub const UPSERT:      &str = "sales_invoice_line:upsert";
}

/// Resource policy for SalesInvoiceLine — maps CRUD actions to permission strings.
pub struct SalesInvoiceLinePolicy;

#[async_trait]
impl ResourcePolicy<SalesInvoiceLine> for SalesInvoiceLinePolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &SalesInvoiceLine,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for SalesInvoiceLine handlers.
///
/// Wraps `SalesInvoiceLineService` and checks `SalesInvoiceLinePolicy` before delegating.
pub type SalesInvoiceLineGuard = PermissionGuard<SalesInvoiceLine>;

/// Service-integrated permission guard for SalesInvoiceLine (for use in generated handlers).
pub type SalesInvoiceLineServiceGuard = ServicePermissionGuard<SalesInvoiceLine, SalesInvoiceLineService, SalesInvoiceLinePolicy>;

// <<< CUSTOM
// END CUSTOM
