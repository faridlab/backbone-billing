//! Auth configuration for Settlement handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<Settlement>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::SettlementService;
use crate::domain::entity::Settlement;

/// Permission string constants for Settlement operations.
pub mod permissions {
    pub const LIST:        &str = "settlement:list";
    pub const READ:        &str = "settlement:read";
    pub const CREATE:      &str = "settlement:create";
    pub const UPDATE:      &str = "settlement:update";
    pub const DELETE:      &str = "settlement:delete";
    pub const RESTORE:     &str = "settlement:restore";
    pub const TRASH:       &str = "settlement:trash";
    pub const EMPTY_TRASH: &str = "settlement:empty_trash";
    pub const BULK_CREATE: &str = "settlement:bulk_create";
    pub const UPSERT:      &str = "settlement:upsert";
}

/// Resource policy for Settlement — maps CRUD actions to permission strings.
pub struct SettlementPolicy;

#[async_trait]
impl ResourcePolicy<Settlement> for SettlementPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &Settlement,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for Settlement handlers.
///
/// Wraps `SettlementService` and checks `SettlementPolicy` before delegating.
pub type SettlementGuard = PermissionGuard<Settlement, SettlementService, SettlementPolicy>;

/// Service-integrated permission guard for Settlement (for use in generated handlers).
pub type SettlementServiceGuard = ServicePermissionGuard<Settlement, SettlementService, SettlementPolicy>;

// <<< CUSTOM
// END CUSTOM
