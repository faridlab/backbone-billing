//! Auth configuration for RefundRequest handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<RefundRequest>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::RefundRequestService;
use crate::domain::entity::RefundRequest;

/// Permission string constants for RefundRequest operations.
pub mod permissions {
    pub const LIST:        &str = "refund_request:list";
    pub const READ:        &str = "refund_request:read";
    pub const CREATE:      &str = "refund_request:create";
    pub const UPDATE:      &str = "refund_request:update";
    pub const DELETE:      &str = "refund_request:delete";
    pub const RESTORE:     &str = "refund_request:restore";
    pub const TRASH:       &str = "refund_request:trash";
    pub const EMPTY_TRASH: &str = "refund_request:empty_trash";
    pub const BULK_CREATE: &str = "refund_request:bulk_create";
    pub const UPSERT:      &str = "refund_request:upsert";
}

/// Resource policy for RefundRequest — maps CRUD actions to permission strings.
pub struct RefundRequestPolicy;

#[async_trait]
impl ResourcePolicy<RefundRequest> for RefundRequestPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &RefundRequest,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for RefundRequest handlers.
///
/// Wraps `RefundRequestService` and checks `RefundRequestPolicy` before delegating.
pub type RefundRequestGuard = PermissionGuard<RefundRequest, RefundRequestService, RefundRequestPolicy>;

/// Service-integrated permission guard for RefundRequest (for use in generated handlers).
pub type RefundRequestServiceGuard = ServicePermissionGuard<RefundRequest, RefundRequestService, RefundRequestPolicy>;

// <<< CUSTOM
// END CUSTOM
