//! Auth configuration for InvoiceParty handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<InvoiceParty>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::InvoicePartyService;
use crate::domain::entity::InvoiceParty;

/// Permission string constants for InvoiceParty operations.
pub mod permissions {
    pub const LIST:        &str = "invoice_party:list";
    pub const READ:        &str = "invoice_party:read";
    pub const CREATE:      &str = "invoice_party:create";
    pub const UPDATE:      &str = "invoice_party:update";
    pub const DELETE:      &str = "invoice_party:delete";
    pub const RESTORE:     &str = "invoice_party:restore";
    pub const TRASH:       &str = "invoice_party:trash";
    pub const EMPTY_TRASH: &str = "invoice_party:empty_trash";
    pub const BULK_CREATE: &str = "invoice_party:bulk_create";
    pub const UPSERT:      &str = "invoice_party:upsert";
}

/// Resource policy for InvoiceParty — maps CRUD actions to permission strings.
pub struct InvoicePartyPolicy;

#[async_trait]
impl ResourcePolicy<InvoiceParty> for InvoicePartyPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &InvoiceParty,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for InvoiceParty handlers.
///
/// Wraps `InvoicePartyService` and checks `InvoicePartyPolicy` before delegating.
pub type InvoicePartyGuard = PermissionGuard<InvoiceParty, InvoicePartyService, InvoicePartyPolicy>;

/// Service-integrated permission guard for InvoiceParty (for use in generated handlers).
pub type InvoicePartyServiceGuard = ServicePermissionGuard<InvoiceParty, InvoicePartyService, InvoicePartyPolicy>;

// <<< CUSTOM
// END CUSTOM
