//! Auth configuration for AgentEarning handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<AgentEarning>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::AgentEarningService;
use crate::domain::entity::AgentEarning;

/// Permission string constants for AgentEarning operations.
pub mod permissions {
    pub const LIST:        &str = "agent_earning:list";
    pub const READ:        &str = "agent_earning:read";
    pub const CREATE:      &str = "agent_earning:create";
    pub const UPDATE:      &str = "agent_earning:update";
    pub const DELETE:      &str = "agent_earning:delete";
    pub const RESTORE:     &str = "agent_earning:restore";
    pub const TRASH:       &str = "agent_earning:trash";
    pub const EMPTY_TRASH: &str = "agent_earning:empty_trash";
    pub const BULK_CREATE: &str = "agent_earning:bulk_create";
    pub const UPSERT:      &str = "agent_earning:upsert";
}

/// Resource policy for AgentEarning — maps CRUD actions to permission strings.
pub struct AgentEarningPolicy;

#[async_trait]
impl ResourcePolicy<AgentEarning> for AgentEarningPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &AgentEarning,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for AgentEarning handlers.
///
/// Wraps `AgentEarningService` and checks `AgentEarningPolicy` before delegating.
pub type AgentEarningGuard = PermissionGuard<AgentEarning, AgentEarningService, AgentEarningPolicy>;

/// Service-integrated permission guard for AgentEarning (for use in generated handlers).
pub type AgentEarningServiceGuard = ServicePermissionGuard<AgentEarning, AgentEarningService, AgentEarningPolicy>;

// <<< CUSTOM
// END CUSTOM
