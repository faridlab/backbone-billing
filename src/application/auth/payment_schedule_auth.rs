//! Auth configuration for PaymentSchedule handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<PaymentSchedule>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::PaymentScheduleService;
use crate::domain::entity::PaymentSchedule;

/// Permission string constants for PaymentSchedule operations.
pub mod permissions {
    pub const LIST:        &str = "payment_schedule:list";
    pub const READ:        &str = "payment_schedule:read";
    pub const CREATE:      &str = "payment_schedule:create";
    pub const UPDATE:      &str = "payment_schedule:update";
    pub const DELETE:      &str = "payment_schedule:delete";
    pub const RESTORE:     &str = "payment_schedule:restore";
    pub const TRASH:       &str = "payment_schedule:trash";
    pub const EMPTY_TRASH: &str = "payment_schedule:empty_trash";
    pub const BULK_CREATE: &str = "payment_schedule:bulk_create";
    pub const UPSERT:      &str = "payment_schedule:upsert";
}

/// Resource policy for PaymentSchedule — maps CRUD actions to permission strings.
pub struct PaymentSchedulePolicy;

#[async_trait]
impl ResourcePolicy<PaymentSchedule> for PaymentSchedulePolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &PaymentSchedule,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for PaymentSchedule handlers.
///
/// Wraps `PaymentScheduleService` and checks `PaymentSchedulePolicy` before delegating.
pub type PaymentScheduleGuard = PermissionGuard<PaymentSchedule>;

/// Service-integrated permission guard for PaymentSchedule (for use in generated handlers).
pub type PaymentScheduleServiceGuard = ServicePermissionGuard<PaymentSchedule, PaymentScheduleService, PaymentSchedulePolicy>;

// <<< CUSTOM
// END CUSTOM
