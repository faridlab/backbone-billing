//! Application service for Payment entities
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Type alias over `GenericCrudService` — all CRUD logic lives in backbone-core.
//! For custom behaviour, create `payment_service_custom.rs` (preserved on regen).

use backbone_core::GenericCrudService;

use crate::presentation::dto::{CreatePaymentDto, UpdatePaymentDto};
use crate::domain::entity::Payment;
use crate::infrastructure::persistence::PaymentRepository;

/// Application service for Payment entities.
///
/// All 12 standard CRUD operations (list, create, get, update, patch,
/// soft_delete, restore, empty_trash, bulk_create, upsert, find_by_id,
/// list_deleted) are provided by `GenericCrudService`.
///
/// Custom extensions go in `payment_service_custom.rs`:
///
/// ```rust,ignore
/// pub struct PaymentServiceCustom {
///     inner: Arc<PaymentService>,
///     // add domain-specific dependencies here
/// }
/// ```
pub type PaymentService = GenericCrudService<
    Payment,
    CreatePaymentDto,
    UpdatePaymentDto,
    PaymentRepository,
>;

// <<< CUSTOM
// END CUSTOM
