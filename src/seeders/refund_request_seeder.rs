//! RefundRequest seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial RefundRequest data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// REFUNDREQUEST SEEDER
// ============================================================================

/// Seeder for refund_request
pub struct SeedRefundRequestSeeder;

impl SeedRefundRequestSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedRefundRequestSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedRefundRequestSeeder {
    fn name(&self) -> &'static str {
        "SeedRefundRequestSeeder"
    }

    fn order(&self) -> i32 {
        10
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if refund_requests table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM refund_requests")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/refund_request_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM refund_requests")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

