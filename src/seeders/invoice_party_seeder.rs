//! InvoiceParty seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial InvoiceParty data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// INVOICEPARTY SEEDER
// ============================================================================

/// Seeder for invoice_party
pub struct SeedInvoicePartySeeder;

impl SeedInvoicePartySeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedInvoicePartySeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedInvoicePartySeeder {
    fn name(&self) -> &'static str {
        "SeedInvoicePartySeeder"
    }

    fn order(&self) -> i32 {
        7
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if invoice_parties table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM invoice_parties")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/invoice_party_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM invoice_parties")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

