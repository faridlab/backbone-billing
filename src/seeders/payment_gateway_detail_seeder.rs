//! PaymentGatewayDetail seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial PaymentGatewayDetail data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// PAYMENTGATEWAYDETAIL SEEDER
// ============================================================================

/// Seeder for payment_gateway_detail
pub struct SeedPaymentGatewayDetailSeeder;

impl SeedPaymentGatewayDetailSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedPaymentGatewayDetailSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedPaymentGatewayDetailSeeder {
    fn name(&self) -> &'static str {
        "SeedPaymentGatewayDetailSeeder"
    }

    fn order(&self) -> i32 {
        4
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if payment_gateway_details table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM payment_gateway_details")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/payment_gateway_detail_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM payment_gateway_details")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

