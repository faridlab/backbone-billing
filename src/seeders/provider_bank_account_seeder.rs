//! ProviderBankAccount seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial ProviderBankAccount data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// PROVIDERBANKACCOUNT SEEDER
// ============================================================================

/// Seeder for provider_bank_account
pub struct SeedProviderBankAccountSeeder;

impl SeedProviderBankAccountSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedProviderBankAccountSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedProviderBankAccountSeeder {
    fn name(&self) -> &'static str {
        "SeedProviderBankAccountSeeder"
    }

    fn order(&self) -> i32 {
        9
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if provider_bank_accounts table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM provider_bank_accounts")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/provider_bank_account_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM provider_bank_accounts")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

