//! AgentEarning seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial AgentEarning data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// AGENTEARNING SEEDER
// ============================================================================

/// Seeder for agent_earning
pub struct SeedAgentEarningSeeder;

impl SeedAgentEarningSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedAgentEarningSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedAgentEarningSeeder {
    fn name(&self) -> &'static str {
        "SeedAgentEarningSeeder"
    }

    fn order(&self) -> i32 {
        1
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if agent_earnings table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM agent_earnings")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/agent_earning_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM agent_earnings")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

