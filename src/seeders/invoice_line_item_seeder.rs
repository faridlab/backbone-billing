//! InvoiceLineItem seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial InvoiceLineItem data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// INVOICELINEITEM SEEDER
// ============================================================================

/// Seeder for invoice_line_item
pub struct SeedInvoiceLineItemSeeder;

impl SeedInvoiceLineItemSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedInvoiceLineItemSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedInvoiceLineItemSeeder {
    fn name(&self) -> &'static str {
        "SeedInvoiceLineItemSeeder"
    }

    fn order(&self) -> i32 {
        8
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if invoice_line_items table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM invoice_line_items")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/invoice_line_item_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM invoice_line_items")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

