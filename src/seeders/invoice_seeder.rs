//! Invoice seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial Invoice data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// INVOICE SEEDER
// ============================================================================

/// Seeder for invoice
pub struct SeedInvoiceSeeder;

impl SeedInvoiceSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedInvoiceSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedInvoiceSeeder {
    fn name(&self) -> &'static str {
        "SeedInvoiceSeeder"
    }

    fn order(&self) -> i32 {
        6
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if invoices table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM invoices")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/invoice_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM invoices")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

