//! PaymentFee seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial PaymentFee data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// PAYMENTFEE SEEDER
// ============================================================================

/// Seeder for payment_fee
pub struct SeedPaymentFeeSeeder;

impl SeedPaymentFeeSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedPaymentFeeSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedPaymentFeeSeeder {
    fn name(&self) -> &'static str {
        "SeedPaymentFeeSeeder"
    }

    fn order(&self) -> i32 {
        5
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if payment_fees table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM payment_fees")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/payment_fee_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM payment_fees")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

