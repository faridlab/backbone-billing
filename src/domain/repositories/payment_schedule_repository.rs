//! Repository trait for PaymentSchedule aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the PaymentSchedule aggregate.
//! Implementation is in the infrastructure layer.

use anyhow::Result;
use async_trait::async_trait;
use uuid::Uuid;

use crate::domain::entity::{InvoiceKind, PaymentSchedule, PaymentScheduleStatus};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PaymentSchedulePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl PaymentSchedulePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct PaymentSchedulePaginatedResult {
    pub data: Vec<PaymentSchedule>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PaymentScheduleFilter {
    pub invoice_ref: Option<Uuid>,
    pub invoice_kind: Option<InvoiceKind>,
    pub company_id: Option<Uuid>,
    pub status: Option<PaymentScheduleStatus>,
}

impl PaymentScheduleFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.invoice_ref.is_some()
            || self.invoice_kind.is_some()
            || self.company_id.is_some()
            || self.status.is_some()
    }
}

/// Repository trait for PaymentSchedule aggregate
///
/// This trait defines the persistence contract for PaymentSchedule entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PaymentScheduleRepository: Send + Sync {
    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new payment_schedule entity
    async fn save(&self, entity: &PaymentSchedule) -> Result<PaymentSchedule>;

    /// Find payment_schedule by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<PaymentSchedule>>;

    /// Find all payment_schedule entities
    async fn find_all(&self) -> Result<Vec<PaymentSchedule>>;

    /// Update payment_schedule by ID
    async fn update(&self, id: &str, entity: &PaymentSchedule) -> Result<Option<PaymentSchedule>>;

    /// Delete payment_schedule by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List payment_schedule with pagination
    async fn list(
        &self,
        params: PaymentSchedulePaginationParams,
    ) -> Result<PaymentSchedulePaginatedResult>;

    /// List payment_schedule with pagination and filters
    async fn list_with_filters(
        &self,
        params: PaymentSchedulePaginationParams,
        filters: PaymentScheduleFilter,
    ) -> Result<PaymentSchedulePaginatedResult>;

    /// Count all payment_schedule entities
    async fn count(&self) -> Result<u64>;

    /// Count payment_schedule entities matching filters
    async fn count_with_filters(&self, filters: PaymentScheduleFilter) -> Result<u64>;

    /// Check if payment_schedule exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete payment_schedule by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted payment_schedule by ID
    async fn restore(&self, id: &str) -> Result<Option<PaymentSchedule>>;

    /// List soft-deleted payment_schedule entities
    async fn list_deleted(
        &self,
        params: PaymentSchedulePaginationParams,
    ) -> Result<PaymentSchedulePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save payment_schedule entities
    async fn bulk_save(&self, entities: &[PaymentSchedule]) -> Result<Vec<PaymentSchedule>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
