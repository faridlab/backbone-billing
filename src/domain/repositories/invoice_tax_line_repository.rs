//! Repository trait for InvoiceTaxLine aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the InvoiceTaxLine aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{InvoiceTaxLine, InvoiceKind, TaxBasis};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct InvoiceTaxLinePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl InvoiceTaxLinePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct InvoiceTaxLinePaginatedResult {
    pub data: Vec<InvoiceTaxLine>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct InvoiceTaxLineFilter {
    pub invoice_ref: Option<Uuid>,
    pub invoice_kind: Option<InvoiceKind>,
    pub company_id: Option<Uuid>,
    pub account_id: Option<Uuid>,
    pub basis: Option<TaxBasis>,
    pub description: Option<String>,
}

impl InvoiceTaxLineFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.invoice_ref.is_some() || self.invoice_kind.is_some() || self.company_id.is_some() || self.account_id.is_some() || self.basis.is_some() || self.description.is_some()
    }
}

/// Repository trait for InvoiceTaxLine aggregate
///
/// This trait defines the persistence contract for InvoiceTaxLine entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait InvoiceTaxLineRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new invoice_tax_line entity
    async fn save(&self, entity: &InvoiceTaxLine) -> Result<InvoiceTaxLine>;

    /// Find invoice_tax_line by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<InvoiceTaxLine>>;

    /// Find all invoice_tax_line entities
    async fn find_all(&self) -> Result<Vec<InvoiceTaxLine>>;

    /// Update invoice_tax_line by ID
    async fn update(&self, id: &str, entity: &InvoiceTaxLine) -> Result<Option<InvoiceTaxLine>>;

    /// Delete invoice_tax_line by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List invoice_tax_line with pagination
    async fn list(&self, params: InvoiceTaxLinePaginationParams) -> Result<InvoiceTaxLinePaginatedResult>;

    /// List invoice_tax_line with pagination and filters
    async fn list_with_filters(&self, params: InvoiceTaxLinePaginationParams, filters: InvoiceTaxLineFilter) -> Result<InvoiceTaxLinePaginatedResult>;

    /// Count all invoice_tax_line entities
    async fn count(&self) -> Result<u64>;

    /// Count invoice_tax_line entities matching filters
    async fn count_with_filters(&self, filters: InvoiceTaxLineFilter) -> Result<u64>;

    /// Check if invoice_tax_line exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete invoice_tax_line by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted invoice_tax_line by ID
    async fn restore(&self, id: &str) -> Result<Option<InvoiceTaxLine>>;

    /// List soft-deleted invoice_tax_line entities
    async fn list_deleted(&self, params: InvoiceTaxLinePaginationParams) -> Result<InvoiceTaxLinePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save invoice_tax_line entities
    async fn bulk_save(&self, entities: &[InvoiceTaxLine]) -> Result<Vec<InvoiceTaxLine>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
