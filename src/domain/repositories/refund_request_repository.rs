//! Repository trait for RefundRequest aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the RefundRequest aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{RefundRequest, RefundMethod, RefundReason, RefundStatus};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct RefundRequestPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl RefundRequestPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct RefundRequestPaginatedResult {
    pub data: Vec<RefundRequest>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct RefundRequestFilter {
    pub refund_number: Option<String>,
    pub payment_id: Option<Uuid>,
    pub order_id: Option<Uuid>,
    pub requester_type: Option<String>,
    pub requester_id: Option<Uuid>,
    pub requester_name: Option<String>,
    pub currency: Option<String>,
    pub reason: Option<RefundReason>,
    pub reason_detail: Option<String>,
    pub status: Option<RefundStatus>,
    pub reviewed_by: Option<Uuid>,
    pub review_notes: Option<String>,
    pub approved_by: Option<Uuid>,
    pub approval_notes: Option<String>,
    pub rejected_by: Option<Uuid>,
    pub rejection_reason: Option<String>,
    pub failure_reason: Option<String>,
    pub gateway_refund_id: Option<String>,
    pub gateway_status: Option<String>,
    pub refund_method: Option<RefundMethod>,
    pub bank_name: Option<String>,
    pub account_number: Option<String>,
    pub account_name: Option<String>,
    pub customer_notified: Option<bool>,
    pub notes: Option<String>,
}

impl RefundRequestFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.refund_number.is_some() || self.payment_id.is_some() || self.order_id.is_some() || self.requester_type.is_some() || self.requester_id.is_some() || self.requester_name.is_some() || self.currency.is_some() || self.reason.is_some() || self.reason_detail.is_some() || self.status.is_some() || self.reviewed_by.is_some() || self.review_notes.is_some() || self.approved_by.is_some() || self.approval_notes.is_some() || self.rejected_by.is_some() || self.rejection_reason.is_some() || self.failure_reason.is_some() || self.gateway_refund_id.is_some() || self.gateway_status.is_some() || self.refund_method.is_some() || self.bank_name.is_some() || self.account_number.is_some() || self.account_name.is_some() || self.customer_notified.is_some() || self.notes.is_some()
    }
}

/// Repository trait for RefundRequest aggregate
///
/// This trait defines the persistence contract for RefundRequest entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait RefundRequestRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new refund_request entity
    async fn save(&self, entity: &RefundRequest) -> Result<RefundRequest>;

    /// Find refund_request by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<RefundRequest>>;

    /// Find all refund_request entities
    async fn find_all(&self) -> Result<Vec<RefundRequest>>;

    /// Update refund_request by ID
    async fn update(&self, id: &str, entity: &RefundRequest) -> Result<Option<RefundRequest>>;

    /// Delete refund_request by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List refund_request with pagination
    async fn list(&self, params: RefundRequestPaginationParams) -> Result<RefundRequestPaginatedResult>;

    /// List refund_request with pagination and filters
    async fn list_with_filters(&self, params: RefundRequestPaginationParams, filters: RefundRequestFilter) -> Result<RefundRequestPaginatedResult>;

    /// Count all refund_request entities
    async fn count(&self) -> Result<u64>;

    /// Count refund_request entities matching filters
    async fn count_with_filters(&self, filters: RefundRequestFilter) -> Result<u64>;

    /// Check if refund_request exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete refund_request by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted refund_request by ID
    async fn restore(&self, id: &str) -> Result<Option<RefundRequest>>;

    /// List soft-deleted refund_request entities
    async fn list_deleted(&self, params: RefundRequestPaginationParams) -> Result<RefundRequestPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find refund_request by refund_number
    async fn find_by_refund_number(&self, refund_number: &str) -> Result<Option<RefundRequest>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if refund_request exists by refund_number
    async fn exists_by_refund_number(&self, refund_number: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save refund_request entities
    async fn bulk_save(&self, entities: &[RefundRequest]) -> Result<Vec<RefundRequest>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
