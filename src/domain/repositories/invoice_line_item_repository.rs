//! Repository trait for InvoiceLineItem aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the InvoiceLineItem aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::InvoiceLineItem;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct InvoiceLineItemPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl InvoiceLineItemPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct InvoiceLineItemPaginatedResult {
    pub data: Vec<InvoiceLineItem>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct InvoiceLineItemFilter {
    pub invoice_id: Option<Uuid>,
    pub description: Option<String>,
}

impl InvoiceLineItemFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.invoice_id.is_some() || self.description.is_some()
    }
}

/// Repository trait for InvoiceLineItem aggregate
///
/// This trait defines the persistence contract for InvoiceLineItem entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait InvoiceLineItemRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new invoice_line_item entity
    async fn save(&self, entity: &InvoiceLineItem) -> Result<InvoiceLineItem>;

    /// Find invoice_line_item by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<InvoiceLineItem>>;

    /// Find all invoice_line_item entities
    async fn find_all(&self) -> Result<Vec<InvoiceLineItem>>;

    /// Update invoice_line_item by ID
    async fn update(&self, id: &str, entity: &InvoiceLineItem) -> Result<Option<InvoiceLineItem>>;

    /// Delete invoice_line_item by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List invoice_line_item with pagination
    async fn list(&self, params: InvoiceLineItemPaginationParams) -> Result<InvoiceLineItemPaginatedResult>;

    /// List invoice_line_item with pagination and filters
    async fn list_with_filters(&self, params: InvoiceLineItemPaginationParams, filters: InvoiceLineItemFilter) -> Result<InvoiceLineItemPaginatedResult>;

    /// Count all invoice_line_item entities
    async fn count(&self) -> Result<u64>;

    /// Count invoice_line_item entities matching filters
    async fn count_with_filters(&self, filters: InvoiceLineItemFilter) -> Result<u64>;

    /// Check if invoice_line_item exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete invoice_line_item by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted invoice_line_item by ID
    async fn restore(&self, id: &str) -> Result<Option<InvoiceLineItem>>;

    /// List soft-deleted invoice_line_item entities
    async fn list_deleted(&self, params: InvoiceLineItemPaginationParams) -> Result<InvoiceLineItemPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save invoice_line_item entities
    async fn bulk_save(&self, entities: &[InvoiceLineItem]) -> Result<Vec<InvoiceLineItem>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
