//! Repository trait for SalesInvoiceLine aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the SalesInvoiceLine aggregate.
//! Implementation is in the infrastructure layer.

use anyhow::Result;
use async_trait::async_trait;
use uuid::Uuid;

use crate::domain::entity::SalesInvoiceLine;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct SalesInvoiceLinePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl SalesInvoiceLinePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct SalesInvoiceLinePaginatedResult {
    pub data: Vec<SalesInvoiceLine>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct SalesInvoiceLineFilter {
    pub invoice_id: Option<Uuid>,
    pub company_id: Option<Uuid>,
    pub item_id: Option<Uuid>,
    pub revenue_account_id: Option<Uuid>,
    pub description: Option<String>,
}

impl SalesInvoiceLineFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.invoice_id.is_some()
            || self.company_id.is_some()
            || self.item_id.is_some()
            || self.revenue_account_id.is_some()
            || self.description.is_some()
    }
}

/// Repository trait for SalesInvoiceLine aggregate
///
/// This trait defines the persistence contract for SalesInvoiceLine entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait SalesInvoiceLineRepository: Send + Sync {
    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new sales_invoice_line entity
    async fn save(&self, entity: &SalesInvoiceLine) -> Result<SalesInvoiceLine>;

    /// Find sales_invoice_line by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<SalesInvoiceLine>>;

    /// Find all sales_invoice_line entities
    async fn find_all(&self) -> Result<Vec<SalesInvoiceLine>>;

    /// Update sales_invoice_line by ID
    async fn update(&self, id: &str, entity: &SalesInvoiceLine)
        -> Result<Option<SalesInvoiceLine>>;

    /// Delete sales_invoice_line by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List sales_invoice_line with pagination
    async fn list(
        &self,
        params: SalesInvoiceLinePaginationParams,
    ) -> Result<SalesInvoiceLinePaginatedResult>;

    /// List sales_invoice_line with pagination and filters
    async fn list_with_filters(
        &self,
        params: SalesInvoiceLinePaginationParams,
        filters: SalesInvoiceLineFilter,
    ) -> Result<SalesInvoiceLinePaginatedResult>;

    /// Count all sales_invoice_line entities
    async fn count(&self) -> Result<u64>;

    /// Count sales_invoice_line entities matching filters
    async fn count_with_filters(&self, filters: SalesInvoiceLineFilter) -> Result<u64>;

    /// Check if sales_invoice_line exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete sales_invoice_line by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted sales_invoice_line by ID
    async fn restore(&self, id: &str) -> Result<Option<SalesInvoiceLine>>;

    /// List soft-deleted sales_invoice_line entities
    async fn list_deleted(
        &self,
        params: SalesInvoiceLinePaginationParams,
    ) -> Result<SalesInvoiceLinePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save sales_invoice_line entities
    async fn bulk_save(&self, entities: &[SalesInvoiceLine]) -> Result<Vec<SalesInvoiceLine>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
