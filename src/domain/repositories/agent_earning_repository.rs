//! Repository trait for AgentEarning aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the AgentEarning aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{AgentEarning, EarningStatus, EarningType};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct AgentEarningPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl AgentEarningPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct AgentEarningPaginatedResult {
    pub data: Vec<AgentEarning>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct AgentEarningFilter {
    pub agent_id: Option<Uuid>,
    pub task_id: Option<Uuid>,
    pub order_id: Option<Uuid>,
    pub earning_type: Option<EarningType>,
    pub description: Option<String>,
    pub currency: Option<String>,
    pub deduction_reason: Option<String>,
    pub status: Option<EarningStatus>,
    pub is_paid: Option<bool>,
    pub payout_id: Option<Uuid>,
    pub cod_deposited: Option<bool>,
}

impl AgentEarningFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.agent_id.is_some() || self.task_id.is_some() || self.order_id.is_some() || self.earning_type.is_some() || self.description.is_some() || self.currency.is_some() || self.deduction_reason.is_some() || self.status.is_some() || self.is_paid.is_some() || self.payout_id.is_some() || self.cod_deposited.is_some()
    }
}

/// Repository trait for AgentEarning aggregate
///
/// This trait defines the persistence contract for AgentEarning entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait AgentEarningRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new agent_earning entity
    async fn save(&self, entity: &AgentEarning) -> Result<AgentEarning>;

    /// Find agent_earning by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<AgentEarning>>;

    /// Find all agent_earning entities
    async fn find_all(&self) -> Result<Vec<AgentEarning>>;

    /// Update agent_earning by ID
    async fn update(&self, id: &str, entity: &AgentEarning) -> Result<Option<AgentEarning>>;

    /// Delete agent_earning by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List agent_earning with pagination
    async fn list(&self, params: AgentEarningPaginationParams) -> Result<AgentEarningPaginatedResult>;

    /// List agent_earning with pagination and filters
    async fn list_with_filters(&self, params: AgentEarningPaginationParams, filters: AgentEarningFilter) -> Result<AgentEarningPaginatedResult>;

    /// Count all agent_earning entities
    async fn count(&self) -> Result<u64>;

    /// Count agent_earning entities matching filters
    async fn count_with_filters(&self, filters: AgentEarningFilter) -> Result<u64>;

    /// Check if agent_earning exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete agent_earning by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted agent_earning by ID
    async fn restore(&self, id: &str) -> Result<Option<AgentEarning>>;

    /// List soft-deleted agent_earning entities
    async fn list_deleted(&self, params: AgentEarningPaginationParams) -> Result<AgentEarningPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save agent_earning entities
    async fn bulk_save(&self, entities: &[AgentEarning]) -> Result<Vec<AgentEarning>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
