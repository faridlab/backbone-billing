//! Repository trait for ProviderBankAccount aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the ProviderBankAccount aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{ProviderBankAccount, BankAccountStatus, BankAccountType, VerificationMethod};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ProviderBankAccountPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl ProviderBankAccountPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct ProviderBankAccountPaginatedResult {
    pub data: Vec<ProviderBankAccount>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ProviderBankAccountFilter {
    pub provider_id: Option<Uuid>,
    pub bank_code: Option<String>,
    pub bank_name: Option<String>,
    pub account_number: Option<String>,
    pub account_name: Option<String>,
    pub account_type: Option<BankAccountType>,
    pub branch_name: Option<String>,
    pub branch_code: Option<String>,
    pub swift_code: Option<String>,
    pub status: Option<BankAccountStatus>,
    pub is_verified: Option<bool>,
    pub verified_by: Option<Uuid>,
    pub verification_method: Option<VerificationMethod>,
    pub verification_notes: Option<String>,
    pub rejection_reason: Option<String>,
    pub rejected_by: Option<Uuid>,
    pub is_primary: Option<bool>,
    pub is_active: Option<bool>,
    pub gateway_account_id: Option<String>,
    pub gateway_status: Option<String>,
    pub document_url: Option<String>,
    pub document_type: Option<String>,
    pub notes: Option<String>,
}

impl ProviderBankAccountFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.provider_id.is_some() || self.bank_code.is_some() || self.bank_name.is_some() || self.account_number.is_some() || self.account_name.is_some() || self.account_type.is_some() || self.branch_name.is_some() || self.branch_code.is_some() || self.swift_code.is_some() || self.status.is_some() || self.is_verified.is_some() || self.verified_by.is_some() || self.verification_method.is_some() || self.verification_notes.is_some() || self.rejection_reason.is_some() || self.rejected_by.is_some() || self.is_primary.is_some() || self.is_active.is_some() || self.gateway_account_id.is_some() || self.gateway_status.is_some() || self.document_url.is_some() || self.document_type.is_some() || self.notes.is_some()
    }
}

/// Repository trait for ProviderBankAccount aggregate
///
/// This trait defines the persistence contract for ProviderBankAccount entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait ProviderBankAccountRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new provider_bank_account entity
    async fn save(&self, entity: &ProviderBankAccount) -> Result<ProviderBankAccount>;

    /// Find provider_bank_account by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<ProviderBankAccount>>;

    /// Find all provider_bank_account entities
    async fn find_all(&self) -> Result<Vec<ProviderBankAccount>>;

    /// Update provider_bank_account by ID
    async fn update(&self, id: &str, entity: &ProviderBankAccount) -> Result<Option<ProviderBankAccount>>;

    /// Delete provider_bank_account by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List provider_bank_account with pagination
    async fn list(&self, params: ProviderBankAccountPaginationParams) -> Result<ProviderBankAccountPaginatedResult>;

    /// List provider_bank_account with pagination and filters
    async fn list_with_filters(&self, params: ProviderBankAccountPaginationParams, filters: ProviderBankAccountFilter) -> Result<ProviderBankAccountPaginatedResult>;

    /// Count all provider_bank_account entities
    async fn count(&self) -> Result<u64>;

    /// Count provider_bank_account entities matching filters
    async fn count_with_filters(&self, filters: ProviderBankAccountFilter) -> Result<u64>;

    /// Check if provider_bank_account exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete provider_bank_account by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted provider_bank_account by ID
    async fn restore(&self, id: &str) -> Result<Option<ProviderBankAccount>>;

    /// List soft-deleted provider_bank_account entities
    async fn list_deleted(&self, params: ProviderBankAccountPaginationParams) -> Result<ProviderBankAccountPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save provider_bank_account entities
    async fn bulk_save(&self, entities: &[ProviderBankAccount]) -> Result<Vec<ProviderBankAccount>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
