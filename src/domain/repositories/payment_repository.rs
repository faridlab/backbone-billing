//! Repository trait for Payment aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the Payment aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{Payment, PayableType, PayeeType, PayerType, PaymentChannel, PaymentGateway, PaymentTransactionStatus};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PaymentPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl PaymentPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct PaymentPaginatedResult {
    pub data: Vec<Payment>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PaymentFilter {
    pub payment_number: Option<String>,
    pub payable_type: Option<PayableType>,
    pub payable_id: Option<Uuid>,
    pub order_id: Option<Uuid>,
    pub payer_type: Option<PayerType>,
    pub payer_id: Option<Uuid>,
    pub payee_type: Option<PayeeType>,
    pub payee_id: Option<Uuid>,
    pub currency: Option<String>,
    pub gateway: Option<PaymentGateway>,
    pub channel: Option<PaymentChannel>,
    pub status: Option<PaymentTransactionStatus>,
    pub failure_code: Option<String>,
    pub failure_message: Option<String>,
    pub settlement_id: Option<Uuid>,
    pub description: Option<String>,
    pub notes: Option<String>,
}

impl PaymentFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.payment_number.is_some() || self.payable_type.is_some() || self.payable_id.is_some() || self.order_id.is_some() || self.payer_type.is_some() || self.payer_id.is_some() || self.payee_type.is_some() || self.payee_id.is_some() || self.currency.is_some() || self.gateway.is_some() || self.channel.is_some() || self.status.is_some() || self.failure_code.is_some() || self.failure_message.is_some() || self.settlement_id.is_some() || self.description.is_some() || self.notes.is_some()
    }
}

/// Repository trait for Payment aggregate
///
/// This trait defines the persistence contract for Payment entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PaymentRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new payment entity
    async fn save(&self, entity: &Payment) -> Result<Payment>;

    /// Find payment by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<Payment>>;

    /// Find all payment entities
    async fn find_all(&self) -> Result<Vec<Payment>>;

    /// Update payment by ID
    async fn update(&self, id: &str, entity: &Payment) -> Result<Option<Payment>>;

    /// Delete payment by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List payment with pagination
    async fn list(&self, params: PaymentPaginationParams) -> Result<PaymentPaginatedResult>;

    /// List payment with pagination and filters
    async fn list_with_filters(&self, params: PaymentPaginationParams, filters: PaymentFilter) -> Result<PaymentPaginatedResult>;

    /// Count all payment entities
    async fn count(&self) -> Result<u64>;

    /// Count payment entities matching filters
    async fn count_with_filters(&self, filters: PaymentFilter) -> Result<u64>;

    /// Check if payment exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete payment by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted payment by ID
    async fn restore(&self, id: &str) -> Result<Option<Payment>>;

    /// List soft-deleted payment entities
    async fn list_deleted(&self, params: PaymentPaginationParams) -> Result<PaymentPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find payment by payment_number
    async fn find_by_payment_number(&self, payment_number: &str) -> Result<Option<Payment>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if payment exists by payment_number
    async fn exists_by_payment_number(&self, payment_number: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save payment entities
    async fn bulk_save(&self, entities: &[Payment]) -> Result<Vec<Payment>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
