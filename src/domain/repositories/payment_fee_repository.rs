//! Repository trait for PaymentFee aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the PaymentFee aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{PaymentFee, PaymentFeeType};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PaymentFeePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl PaymentFeePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct PaymentFeePaginatedResult {
    pub data: Vec<PaymentFee>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PaymentFeeFilter {
    pub payment_id: Option<Uuid>,
    pub fee_type: Option<PaymentFeeType>,
    pub description: Option<String>,
}

impl PaymentFeeFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.payment_id.is_some() || self.fee_type.is_some() || self.description.is_some()
    }
}

/// Repository trait for PaymentFee aggregate
///
/// This trait defines the persistence contract for PaymentFee entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PaymentFeeRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new payment_fee entity
    async fn save(&self, entity: &PaymentFee) -> Result<PaymentFee>;

    /// Find payment_fee by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<PaymentFee>>;

    /// Find all payment_fee entities
    async fn find_all(&self) -> Result<Vec<PaymentFee>>;

    /// Update payment_fee by ID
    async fn update(&self, id: &str, entity: &PaymentFee) -> Result<Option<PaymentFee>>;

    /// Delete payment_fee by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List payment_fee with pagination
    async fn list(&self, params: PaymentFeePaginationParams) -> Result<PaymentFeePaginatedResult>;

    /// List payment_fee with pagination and filters
    async fn list_with_filters(&self, params: PaymentFeePaginationParams, filters: PaymentFeeFilter) -> Result<PaymentFeePaginatedResult>;

    /// Count all payment_fee entities
    async fn count(&self) -> Result<u64>;

    /// Count payment_fee entities matching filters
    async fn count_with_filters(&self, filters: PaymentFeeFilter) -> Result<u64>;

    /// Check if payment_fee exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete payment_fee by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted payment_fee by ID
    async fn restore(&self, id: &str) -> Result<Option<PaymentFee>>;

    /// List soft-deleted payment_fee entities
    async fn list_deleted(&self, params: PaymentFeePaginationParams) -> Result<PaymentFeePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save payment_fee entities
    async fn bulk_save(&self, entities: &[PaymentFee]) -> Result<Vec<PaymentFee>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
