//! Repository trait for PurchaseInvoice aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the PurchaseInvoice aggregate.
//! Implementation is in the infrastructure layer.

use anyhow::Result;
use async_trait::async_trait;
use uuid::Uuid;

use crate::domain::entity::{GlPostingState, InvoiceStatus, PurchaseInvoice};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PurchaseInvoicePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl PurchaseInvoicePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct PurchaseInvoicePaginatedResult {
    pub data: Vec<PurchaseInvoice>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PurchaseInvoiceFilter {
    pub invoice_number: Option<String>,
    pub company_id: Option<Uuid>,
    pub branch_id: Option<Uuid>,
    pub supplier_id: Option<Uuid>,
    pub source_po_id: Option<Uuid>,
    pub status: Option<InvoiceStatus>,
    pub currency: Option<String>,
    pub payable_account_id: Option<Uuid>,
    pub posting_state: Option<GlPostingState>,
    pub journal_id: Option<Uuid>,
    pub accounting_post_id: Option<Uuid>,
    pub notes: Option<String>,
}

impl PurchaseInvoiceFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.invoice_number.is_some()
            || self.company_id.is_some()
            || self.branch_id.is_some()
            || self.supplier_id.is_some()
            || self.source_po_id.is_some()
            || self.status.is_some()
            || self.currency.is_some()
            || self.payable_account_id.is_some()
            || self.posting_state.is_some()
            || self.journal_id.is_some()
            || self.accounting_post_id.is_some()
            || self.notes.is_some()
    }
}

/// Repository trait for PurchaseInvoice aggregate
///
/// This trait defines the persistence contract for PurchaseInvoice entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PurchaseInvoiceRepository: Send + Sync {
    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new purchase_invoice entity
    async fn save(&self, entity: &PurchaseInvoice) -> Result<PurchaseInvoice>;

    /// Find purchase_invoice by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<PurchaseInvoice>>;

    /// Find all purchase_invoice entities
    async fn find_all(&self) -> Result<Vec<PurchaseInvoice>>;

    /// Update purchase_invoice by ID
    async fn update(&self, id: &str, entity: &PurchaseInvoice) -> Result<Option<PurchaseInvoice>>;

    /// Delete purchase_invoice by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List purchase_invoice with pagination
    async fn list(
        &self,
        params: PurchaseInvoicePaginationParams,
    ) -> Result<PurchaseInvoicePaginatedResult>;

    /// List purchase_invoice with pagination and filters
    async fn list_with_filters(
        &self,
        params: PurchaseInvoicePaginationParams,
        filters: PurchaseInvoiceFilter,
    ) -> Result<PurchaseInvoicePaginatedResult>;

    /// Count all purchase_invoice entities
    async fn count(&self) -> Result<u64>;

    /// Count purchase_invoice entities matching filters
    async fn count_with_filters(&self, filters: PurchaseInvoiceFilter) -> Result<u64>;

    /// Check if purchase_invoice exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete purchase_invoice by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted purchase_invoice by ID
    async fn restore(&self, id: &str) -> Result<Option<PurchaseInvoice>>;

    /// List soft-deleted purchase_invoice entities
    async fn list_deleted(
        &self,
        params: PurchaseInvoicePaginationParams,
    ) -> Result<PurchaseInvoicePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save purchase_invoice entities
    async fn bulk_save(&self, entities: &[PurchaseInvoice]) -> Result<Vec<PurchaseInvoice>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
