//! Domain events for Settlement entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Settlement;

/// Domain events for Settlement — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Settlement>)` variant.
pub type SettlementEvent = CrudEvent<Settlement>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Settlement events.
pub type SettlementEventPublisher = Arc<dyn CrudEventPublisher<Settlement> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopSettlementEventPublisher = NoOpCrudEventPublisher<Settlement>;
