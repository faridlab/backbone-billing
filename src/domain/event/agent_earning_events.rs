//! Domain events for AgentEarning entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::AgentEarning;

/// Domain events for AgentEarning — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<AgentEarning>)` variant.
pub type AgentEarningEvent = CrudEvent<AgentEarning>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for AgentEarning events.
pub type AgentEarningEventPublisher = Arc<dyn CrudEventPublisher<AgentEarning> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopAgentEarningEventPublisher = NoOpCrudEventPublisher<AgentEarning>;
