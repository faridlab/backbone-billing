//! Domain events for PaymentTermLine entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use crate::domain::entity::PaymentTermLine;
use backbone_messaging::CrudEvent;

/// Domain events for PaymentTermLine — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<PaymentTermLine>)` variant.
pub type PaymentTermLineEvent = CrudEvent<PaymentTermLine>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for PaymentTermLine events.
pub type PaymentTermLineEventPublisher = Arc<dyn CrudEventPublisher<PaymentTermLine> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopPaymentTermLineEventPublisher = NoOpCrudEventPublisher<PaymentTermLine>;
