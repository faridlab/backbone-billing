//! Domain events for Payment entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Payment;

/// Domain events for Payment — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Payment>)` variant.
pub type PaymentEvent = CrudEvent<Payment>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Payment events.
pub type PaymentEventPublisher = Arc<dyn CrudEventPublisher<Payment> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopPaymentEventPublisher = NoOpCrudEventPublisher<Payment>;
