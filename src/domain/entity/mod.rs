//! Domain Entities
//!
//! Generated by metaphor-schema. Do not edit manually.

pub mod discount_tax_basis;
pub mod gl_posting_state;
pub mod invoice_kind;
pub mod invoice_status;
pub mod invoice_tax_line;
pub mod payment_schedule;
pub mod payment_schedule_status;
pub mod payment_term;
pub mod payment_term_anchor;
pub mod payment_term_delay_type;
pub mod payment_term_line;
pub mod payment_term_line_value;
pub mod payment_term_status;
pub mod purchase_invoice;
pub mod purchase_invoice_line;
pub mod sales_invoice;
pub mod sales_invoice_line;
pub mod tax_basis;
pub mod tax_exigibility;

// Re-exports
pub use discount_tax_basis::DiscountTaxBasis;
pub use gl_posting_state::GlPostingState;
pub use invoice_kind::InvoiceKind;
pub use invoice_status::InvoiceStatus;
pub use invoice_tax_line::InvoiceTaxLine;
pub use invoice_tax_line::InvoiceTaxLineBuilder;
pub use invoice_tax_line::InvoiceTaxLineId;
pub use payment_schedule::PaymentSchedule;
pub use payment_schedule::PaymentScheduleBuilder;
pub use payment_schedule::PaymentScheduleId;
pub use payment_schedule_status::PaymentScheduleStatus;
pub use payment_term::PaymentTerm;
pub use payment_term::PaymentTermBuilder;
pub use payment_term::PaymentTermId;
pub use payment_term_anchor::PaymentTermAnchor;
pub use payment_term_delay_type::PaymentTermDelayType;
pub use payment_term_line::PaymentTermLine;
pub use payment_term_line::PaymentTermLineBuilder;
pub use payment_term_line::PaymentTermLineId;
pub use payment_term_line_value::PaymentTermLineValue;
pub use payment_term_status::PaymentTermStatus;
pub use purchase_invoice::PurchaseInvoice;
pub use purchase_invoice::PurchaseInvoiceBuilder;
pub use purchase_invoice::PurchaseInvoiceId;
pub use purchase_invoice_line::PurchaseInvoiceLine;
pub use purchase_invoice_line::PurchaseInvoiceLineBuilder;
pub use purchase_invoice_line::PurchaseInvoiceLineId;
pub use sales_invoice::SalesInvoice;
pub use sales_invoice::SalesInvoiceBuilder;
pub use sales_invoice::SalesInvoiceId;
pub use sales_invoice_line::SalesInvoiceLine;
pub use sales_invoice_line::SalesInvoiceLineBuilder;
pub use sales_invoice_line::SalesInvoiceLineId;
pub use tax_basis::TaxBasis;
pub use tax_exigibility::TaxExigibility;

// ==========================================================================
// Entity Trait
// ==========================================================================

use std::fmt::Debug;

/// Trait for domain entities
///
/// All generated entities implement this trait, providing
/// a common interface for working with domain objects.
pub trait Entity: Debug + Clone {
    /// The type of the entity's unique identifier
    type Id: Debug + Clone + PartialEq;

    /// Get the entity's unique identifier
    fn entity_id(&self) -> &Self::Id;

    /// Get the entity type name
    fn entity_type() -> &'static str;
}

// ==========================================================================
// Audit Metadata
// ==========================================================================

use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};
#[cfg(feature = "openapi")]
use utoipa::ToSchema;
use uuid::Uuid;

/// Audit metadata stored as JSONB in the database
///
/// This struct consolidates audit fields (timestamps and actors) into a single
/// JSONB column for efficient storage and flexible querying.
#[derive(Debug, Clone, Default, Serialize, Deserialize, PartialEq)]
#[cfg_attr(feature = "openapi", derive(ToSchema))]
pub struct AuditMetadata {
    /// Timestamp when the record was created
    #[serde(skip_serializing_if = "Option::is_none")]
    pub created_at: Option<DateTime<Utc>>,

    /// Timestamp when the record was last updated
    #[serde(skip_serializing_if = "Option::is_none")]
    pub updated_at: Option<DateTime<Utc>>,

    /// Timestamp when the record was soft-deleted
    #[serde(skip_serializing_if = "Option::is_none")]
    pub deleted_at: Option<DateTime<Utc>>,

    /// User ID who created the record
    #[serde(skip_serializing_if = "Option::is_none")]
    pub created_by: Option<Uuid>,

    /// User ID who last updated the record
    #[serde(skip_serializing_if = "Option::is_none")]
    pub updated_by: Option<Uuid>,

    /// User ID who deleted the record
    #[serde(skip_serializing_if = "Option::is_none")]
    pub deleted_by: Option<Uuid>,
}

impl AuditMetadata {
    /// Create new audit metadata with created_at set to now
    pub fn new() -> Self {
        Self {
            created_at: Some(Utc::now()),
            ..Default::default()
        }
    }

    /// Create with creator ID
    pub fn with_creator(creator_id: Uuid) -> Self {
        Self {
            created_at: Some(Utc::now()),
            created_by: Some(creator_id),
            ..Default::default()
        }
    }

    /// Touch the record (update updated_at)
    pub fn touch(&mut self) {
        self.updated_at = Some(Utc::now());
    }

    /// Touch with updater ID
    pub fn touch_by(&mut self, updater_id: Uuid) {
        self.updated_at = Some(Utc::now());
        self.updated_by = Some(updater_id);
    }

    /// Soft delete the record
    pub fn soft_delete(&mut self) {
        self.deleted_at = Some(Utc::now());
    }

    /// Soft delete with deleter ID
    pub fn soft_delete_by(&mut self, deleter_id: Uuid) {
        self.deleted_at = Some(Utc::now());
        self.deleted_by = Some(deleter_id);
    }

    /// Restore a soft-deleted record
    pub fn restore(&mut self) {
        self.deleted_at = None;
        self.deleted_by = None;
    }

    /// Check if record is deleted
    pub fn is_deleted(&self) -> bool {
        self.deleted_at.is_some()
    }

    /// Check if record is active (not deleted)
    pub fn is_active(&self) -> bool {
        self.deleted_at.is_none()
    }
}
