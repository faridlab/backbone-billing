//! Domain Entities
//!
//! Generated by backbone-schema. Do not edit manually.

pub mod agent_earning;
pub mod financial_period;
pub mod payment;
pub mod payment_gateway_detail;
pub mod payment_fee;
pub mod invoice;
pub mod invoice_party;
pub mod invoice_line_item;
pub mod provider_bank_account;
pub mod refund_request;
pub mod settlement;
pub mod earning_type;
pub mod earning_status;
pub mod financial_period_type;
pub mod financial_period_status;
pub mod payable_type;
pub mod payer_type;
pub mod payee_type;
pub mod payment_transaction_status;
pub mod payment_gateway;
pub mod payment_channel;
pub mod payment_fee_type;
pub mod invoiceable_type;
pub mod invoice_type;
pub mod invoice_status;
pub mod invoice_party_role;
pub mod invoice_party_type;
pub mod bank_account_type;
pub mod bank_account_status;
pub mod verification_method;
pub mod refund_method;
pub mod refund_reason;
pub mod refund_status;
pub mod settlement_status;
pub mod settlement_frequency;

// Re-exports
pub use agent_earning::AgentEarning;
pub use agent_earning::AgentEarningBuilder;
pub use agent_earning::AgentEarningId;
pub use financial_period::FinancialPeriod;
pub use financial_period::FinancialPeriodBuilder;
pub use financial_period::FinancialPeriodId;
pub use payment::Payment;
pub use payment::PaymentBuilder;
pub use payment::PaymentId;
pub use payment_gateway_detail::PaymentGatewayDetail;
pub use payment_gateway_detail::PaymentGatewayDetailBuilder;
pub use payment_gateway_detail::PaymentGatewayDetailId;
pub use payment_fee::PaymentFee;
pub use payment_fee::PaymentFeeBuilder;
pub use payment_fee::PaymentFeeId;
pub use invoice::Invoice;
pub use invoice::InvoiceBuilder;
pub use invoice::InvoiceId;
pub use invoice_party::InvoiceParty;
pub use invoice_party::InvoicePartyBuilder;
pub use invoice_party::InvoicePartyId;
pub use invoice_line_item::InvoiceLineItem;
pub use invoice_line_item::InvoiceLineItemBuilder;
pub use invoice_line_item::InvoiceLineItemId;
pub use provider_bank_account::ProviderBankAccount;
pub use provider_bank_account::ProviderBankAccountBuilder;
pub use provider_bank_account::ProviderBankAccountId;
pub use refund_request::RefundRequest;
pub use refund_request::RefundRequestBuilder;
pub use refund_request::RefundRequestId;
pub use settlement::Settlement;
pub use settlement::SettlementBuilder;
pub use settlement::SettlementId;
pub use earning_type::EarningType;
pub use earning_status::EarningStatus;
pub use financial_period_type::FinancialPeriodType;
pub use financial_period_status::FinancialPeriodStatus;
pub use payable_type::PayableType;
pub use payer_type::PayerType;
pub use payee_type::PayeeType;
pub use payment_transaction_status::PaymentTransactionStatus;
pub use payment_gateway::PaymentGateway;
pub use payment_channel::PaymentChannel;
pub use payment_fee_type::PaymentFeeType;
pub use invoiceable_type::InvoiceableType;
pub use invoice_type::InvoiceType;
pub use invoice_status::InvoiceStatus;
pub use invoice_party_role::InvoicePartyRole;
pub use invoice_party_type::InvoicePartyType;
pub use bank_account_type::BankAccountType;
pub use bank_account_status::BankAccountStatus;
pub use verification_method::VerificationMethod;
pub use refund_method::RefundMethod;
pub use refund_reason::RefundReason;
pub use refund_status::RefundStatus;
pub use settlement_status::SettlementStatus;
pub use settlement_frequency::SettlementFrequency;

// ==========================================================================
// Entity Trait
// ==========================================================================

use std::fmt::Debug;

/// Trait for domain entities
///
/// All generated entities implement this trait, providing
/// a common interface for working with domain objects.
pub trait Entity: Debug + Clone {
    /// The type of the entity's unique identifier
    type Id: Debug + Clone + PartialEq;

    /// Get the entity's unique identifier
    fn entity_id(&self) -> &Self::Id;

    /// Get the entity type name
    fn entity_type() -> &'static str;
}

// ==========================================================================
// Audit Metadata
// ==========================================================================

use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};
use uuid::Uuid;

/// Audit metadata stored as JSONB in the database
///
/// This struct consolidates audit fields (timestamps and actors) into a single
/// JSONB column for efficient storage and flexible querying.
#[derive(Debug, Clone, Default, Serialize, Deserialize, PartialEq)]
pub struct AuditMetadata {
    /// Timestamp when the record was created
    #[serde(skip_serializing_if = "Option::is_none")]
    pub created_at: Option<DateTime<Utc>>,

    /// Timestamp when the record was last updated
    #[serde(skip_serializing_if = "Option::is_none")]
    pub updated_at: Option<DateTime<Utc>>,

    /// Timestamp when the record was soft-deleted
    #[serde(skip_serializing_if = "Option::is_none")]
    pub deleted_at: Option<DateTime<Utc>>,

    /// User ID who created the record
    #[serde(skip_serializing_if = "Option::is_none")]
    pub created_by: Option<Uuid>,

    /// User ID who last updated the record
    #[serde(skip_serializing_if = "Option::is_none")]
    pub updated_by: Option<Uuid>,

    /// User ID who deleted the record
    #[serde(skip_serializing_if = "Option::is_none")]
    pub deleted_by: Option<Uuid>,
}

impl AuditMetadata {
    /// Create new audit metadata with created_at set to now
    pub fn new() -> Self {
        Self {
            created_at: Some(Utc::now()),
            ..Default::default()
        }
    }

    /// Create with creator ID
    pub fn with_creator(creator_id: Uuid) -> Self {
        Self {
            created_at: Some(Utc::now()),
            created_by: Some(creator_id),
            ..Default::default()
        }
    }

    /// Touch the record (update updated_at)
    pub fn touch(&mut self) {
        self.updated_at = Some(Utc::now());
    }

    /// Touch with updater ID
    pub fn touch_by(&mut self, updater_id: Uuid) {
        self.updated_at = Some(Utc::now());
        self.updated_by = Some(updater_id);
    }

    /// Soft delete the record
    pub fn soft_delete(&mut self) {
        self.deleted_at = Some(Utc::now());
    }

    /// Soft delete with deleter ID
    pub fn soft_delete_by(&mut self, deleter_id: Uuid) {
        self.deleted_at = Some(Utc::now());
        self.deleted_by = Some(deleter_id);
    }

    /// Restore a soft-deleted record
    pub fn restore(&mut self) {
        self.deleted_at = None;
        self.deleted_by = None;
    }

    /// Check if record is deleted
    pub fn is_deleted(&self) -> bool {
        self.deleted_at.is_some()
    }

    /// Check if record is active (not deleted)
    pub fn is_active(&self) -> bool {
        self.deleted_at.is_none()
    }
}
