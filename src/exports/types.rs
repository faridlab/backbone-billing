//! Public types for Billing module
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! These DTOs are the ONLY types other modules should use.
//! They are decoupled from internal domain entities.

use crate::domain::entity::*;
use chrono::{DateTime, NaiveDate, Utc};
use rust_decimal::Decimal;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

// ============================================================================
// INVOICETAXLINE TYPES
// ============================================================================

/// Type-safe ID for InvoiceTaxLine
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct InvoiceTaxLineId(pub Uuid);

impl InvoiceTaxLineId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for InvoiceTaxLineId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<InvoiceTaxLineId> for Uuid {
    fn from(id: InvoiceTaxLineId) -> Self {
        id.0
    }
}

/// Data transfer object for InvoiceTaxLine
///
/// This is the public representation of InvoiceTaxLine for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct InvoiceTaxLineDto {
    pub id: InvoiceTaxLineId,
    pub invoice_ref: Uuid,
    pub invoice_kind: InvoiceKind,
    pub company_id: Uuid,
    pub account_id: Uuid,
    pub basis: TaxBasis,
    pub description: Option<String>,
    pub taxable_base: Decimal,
    pub rate: Decimal,
    pub tax_amount: Decimal,
    pub tax_template_id: Option<Uuid>,
    pub repartition_line_id: Option<Uuid>,
    pub real_account_id: Option<Uuid>,
    pub exigibility: TaxExigibility,
    pub metadata: serde_json::Value,
}

/// Summary view of InvoiceTaxLine for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct InvoiceTaxLineSummary {
    pub id: InvoiceTaxLineId,
}

/// Reference to InvoiceTaxLine for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct InvoiceTaxLineRef {
    pub id: InvoiceTaxLineId,
}

// ============================================================================
// PAYMENTSCHEDULE TYPES
// ============================================================================

/// Type-safe ID for PaymentSchedule
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct PaymentScheduleId(pub Uuid);

impl PaymentScheduleId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for PaymentScheduleId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<PaymentScheduleId> for Uuid {
    fn from(id: PaymentScheduleId) -> Self {
        id.0
    }
}

/// Data transfer object for PaymentSchedule
///
/// This is the public representation of PaymentSchedule for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PaymentScheduleDto {
    pub id: PaymentScheduleId,
    pub invoice_ref: Uuid,
    pub invoice_kind: InvoiceKind,
    pub company_id: Uuid,
    pub installment_no: i32,
    pub due_date: NaiveDate,
    pub amount: Decimal,
    pub paid_amount: Decimal,
    pub status: PaymentScheduleStatus,
    pub metadata: serde_json::Value,
}

/// Summary view of PaymentSchedule for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PaymentScheduleSummary {
    pub id: PaymentScheduleId,
    pub status: PaymentScheduleStatus,
}

/// Reference to PaymentSchedule for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PaymentScheduleRef {
    pub id: PaymentScheduleId,
}

// ============================================================================
// PAYMENTTERM TYPES
// ============================================================================

/// Type-safe ID for PaymentTerm
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct PaymentTermId(pub Uuid);

impl PaymentTermId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for PaymentTermId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<PaymentTermId> for Uuid {
    fn from(id: PaymentTermId) -> Self {
        id.0
    }
}

/// Data transfer object for PaymentTerm
///
/// This is the public representation of PaymentTerm for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PaymentTermDto {
    pub id: PaymentTermId,
    pub company_id: Option<Uuid>,
    pub name: String,
    pub note: Option<String>,
    pub sequence: i32,
    pub status: PaymentTermStatus,
    pub early_discount: bool,
    pub discount_percent: Decimal,
    pub discount_days: i32,
    pub discount_account_id: Option<Uuid>,
    pub discount_tax_basis: DiscountTaxBasis,
    pub metadata: serde_json::Value,
}

/// Summary view of PaymentTerm for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PaymentTermSummary {
    pub id: PaymentTermId,
    pub name: String,
    pub status: PaymentTermStatus,
}

/// Reference to PaymentTerm for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PaymentTermRef {
    pub id: PaymentTermId,
}

// ============================================================================
// PAYMENTTERMLINE TYPES
// ============================================================================

/// Type-safe ID for PaymentTermLine
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct PaymentTermLineId(pub Uuid);

impl PaymentTermLineId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for PaymentTermLineId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<PaymentTermLineId> for Uuid {
    fn from(id: PaymentTermLineId) -> Self {
        id.0
    }
}

/// Data transfer object for PaymentTermLine
///
/// This is the public representation of PaymentTermLine for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PaymentTermLineDto {
    pub id: PaymentTermLineId,
    pub term_id: Uuid,
    pub company_id: Option<Uuid>,
    pub value: PaymentTermLineValue,
    pub value_amount: Decimal,
    pub nb_days: i32,
    pub day_of_month: Option<i32>,
    pub delay_type: PaymentTermDelayType,
    pub anchor: PaymentTermAnchor,
    pub sequence: i32,
    pub metadata: serde_json::Value,
}

/// Summary view of PaymentTermLine for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PaymentTermLineSummary {
    pub id: PaymentTermLineId,
}

/// Reference to PaymentTermLine for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PaymentTermLineRef {
    pub id: PaymentTermLineId,
}

// ============================================================================
// PURCHASEINVOICE TYPES
// ============================================================================

/// Type-safe ID for PurchaseInvoice
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct PurchaseInvoiceId(pub Uuid);

impl PurchaseInvoiceId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for PurchaseInvoiceId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<PurchaseInvoiceId> for Uuid {
    fn from(id: PurchaseInvoiceId) -> Self {
        id.0
    }
}

/// Data transfer object for PurchaseInvoice
///
/// This is the public representation of PurchaseInvoice for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PurchaseInvoiceDto {
    pub id: PurchaseInvoiceId,
    pub invoice_number: String,
    pub company_id: Uuid,
    pub branch_id: Option<Uuid>,
    pub supplier_id: Uuid,
    pub source_po_id: Option<Uuid>,
    pub status: InvoiceStatus,
    pub posting_date: NaiveDate,
    pub due_date: Option<NaiveDate>,
    pub payment_term_id: Option<Uuid>,
    pub early_pay_discount_percent: Decimal,
    pub early_pay_discount_deadline: Option<NaiveDate>,
    pub early_pay_discount_account_id: Option<Uuid>,
    pub currency: String,
    pub net_total: Decimal,
    pub tax_total: Decimal,
    pub withholding_total: Decimal,
    pub grand_total: Decimal,
    pub outstanding_amount: Decimal,
    pub payable_account_id: Uuid,
    pub posting_state: GlPostingState,
    pub journal_id: Option<Uuid>,
    pub accounting_post_id: Option<Uuid>,
    pub posted_at: Option<DateTime<Utc>>,
    pub notes: Option<String>,
    pub metadata: serde_json::Value,
}

/// Summary view of PurchaseInvoice for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PurchaseInvoiceSummary {
    pub id: PurchaseInvoiceId,
    pub status: InvoiceStatus,
}

/// Reference to PurchaseInvoice for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PurchaseInvoiceRef {
    pub id: PurchaseInvoiceId,
}

// ============================================================================
// PURCHASEINVOICELINE TYPES
// ============================================================================

/// Type-safe ID for PurchaseInvoiceLine
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct PurchaseInvoiceLineId(pub Uuid);

impl PurchaseInvoiceLineId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for PurchaseInvoiceLineId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<PurchaseInvoiceLineId> for Uuid {
    fn from(id: PurchaseInvoiceLineId) -> Self {
        id.0
    }
}

/// Data transfer object for PurchaseInvoiceLine
///
/// This is the public representation of PurchaseInvoiceLine for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PurchaseInvoiceLineDto {
    pub id: PurchaseInvoiceLineId,
    pub invoice_id: Uuid,
    pub company_id: Uuid,
    pub item_id: Uuid,
    pub expense_account_id: Uuid,
    pub description: Option<String>,
    pub quantity: Decimal,
    pub unit_price: Decimal,
    pub net_amount: Decimal,
    pub metadata: serde_json::Value,
}

/// Summary view of PurchaseInvoiceLine for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PurchaseInvoiceLineSummary {
    pub id: PurchaseInvoiceLineId,
}

/// Reference to PurchaseInvoiceLine for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PurchaseInvoiceLineRef {
    pub id: PurchaseInvoiceLineId,
}

// ============================================================================
// SALESINVOICE TYPES
// ============================================================================

/// Type-safe ID for SalesInvoice
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct SalesInvoiceId(pub Uuid);

impl SalesInvoiceId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for SalesInvoiceId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<SalesInvoiceId> for Uuid {
    fn from(id: SalesInvoiceId) -> Self {
        id.0
    }
}

/// Data transfer object for SalesInvoice
///
/// This is the public representation of SalesInvoice for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct SalesInvoiceDto {
    pub id: SalesInvoiceId,
    pub invoice_number: String,
    pub company_id: Uuid,
    pub branch_id: Option<Uuid>,
    pub customer_id: Uuid,
    pub source_so_id: Option<Uuid>,
    pub status: InvoiceStatus,
    pub posting_date: NaiveDate,
    pub due_date: Option<NaiveDate>,
    pub payment_term_id: Option<Uuid>,
    pub early_pay_discount_percent: Decimal,
    pub early_pay_discount_deadline: Option<NaiveDate>,
    pub early_pay_discount_account_id: Option<Uuid>,
    pub currency: String,
    pub net_total: Decimal,
    pub tax_total: Decimal,
    pub grand_total: Decimal,
    pub outstanding_amount: Decimal,
    pub receivable_account_id: Uuid,
    pub posting_state: GlPostingState,
    pub journal_id: Option<Uuid>,
    pub accounting_post_id: Option<Uuid>,
    pub posted_at: Option<DateTime<Utc>>,
    pub notes: Option<String>,
    pub metadata: serde_json::Value,
}

/// Summary view of SalesInvoice for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct SalesInvoiceSummary {
    pub id: SalesInvoiceId,
    pub status: InvoiceStatus,
}

/// Reference to SalesInvoice for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct SalesInvoiceRef {
    pub id: SalesInvoiceId,
}

// ============================================================================
// SALESINVOICELINE TYPES
// ============================================================================

/// Type-safe ID for SalesInvoiceLine
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct SalesInvoiceLineId(pub Uuid);

impl SalesInvoiceLineId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for SalesInvoiceLineId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<SalesInvoiceLineId> for Uuid {
    fn from(id: SalesInvoiceLineId) -> Self {
        id.0
    }
}

/// Data transfer object for SalesInvoiceLine
///
/// This is the public representation of SalesInvoiceLine for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct SalesInvoiceLineDto {
    pub id: SalesInvoiceLineId,
    pub invoice_id: Uuid,
    pub company_id: Uuid,
    pub item_id: Uuid,
    pub revenue_account_id: Uuid,
    pub description: Option<String>,
    pub quantity: Decimal,
    pub unit_price: Decimal,
    pub net_amount: Decimal,
    pub metadata: serde_json::Value,
}

/// Summary view of SalesInvoiceLine for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct SalesInvoiceLineSummary {
    pub id: SalesInvoiceLineId,
}

/// Reference to SalesInvoiceLine for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct SalesInvoiceLineRef {
    pub id: SalesInvoiceLineId,
}

// ============================================================================
// CUSTOM TYPES
// ============================================================================

// <<< CUSTOM TYPES START >>>
// Add custom public types here
// <<< CUSTOM TYPES END >>>
